export default function HomePage() {
  return (
    <main
      style={{
        padding: "40px",
        display: "flex",
        flexDirection: "column",
        gap: "20px",
        margin: "0 auto",
      }}
    >
      <h1 style={{ fontSize: "var(--ev-text-2xl)" }}>
        EV Components Playground
      </h1>
    </main>
  );
}
